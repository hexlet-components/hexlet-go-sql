version: "2"
sql:
  - engine: postgresql
    schema: migrations
    queries:
      - query/users
      - query/courses
      - query/course_members
      - query/lessons
    gen:
      go:
        package: db
        out: internal/db
        emit_json_tags: true
        emit_interface: true
